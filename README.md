# Serendib Studios HTML5 Lab

## Student
Name: [YOUR NAME]
Module: IT22023

## How to run
Open `index.html` in VS Code and use Live Server, or open it directly in a browser.

## Implemented
- Semantic HTML5 structure: header, nav, main, section, article, aside, footer
- Accessible booking form with labels, fieldsets, legends, validation, and ARIA descriptions
- Responsive media patterns: picture, srcset, video, audio, captions, fallbacks
- JSON-LD structured data: Organization, Event, BreadcrumbList
- Basic responsive CSS

## Assets note
The HTML references image, video, and audio files in `assets/`. If real media is not provided, keep the folder structure and replace the placeholder filenames with real files later.

## Audit notes
- HTML should be checked with the Nu HTML Checker / W3C Validator.
- Keyboard test: Tab through navigation, form fields, buttons, video/audio controls.
- Accessibility smoke test: labels, alt text, captions, visible focus styles.
- Any warnings about missing media files are expected if placeholder assets are not supplied.
